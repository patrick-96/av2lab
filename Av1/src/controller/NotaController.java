package controller;

import javax.swing.JTable;
import javax.swing.table.DefaultTableModel;

import model.Nota;
import persistence.NotaDao;

//public class NotaController implements INotaController {
	//private JTable tblTotal;

	//public NotaController(JTable tblTotal) {
	//	this.tblTotal = tblTotal;
	//}

	//@Override
	/*public void tabelaTotal() {
	if(tblTotal!=null){
		DefaultTableModel modelo= (DefaultTableModel) tblTotal.getModel()
	if(modelo.getRowCount()>0){
		modelo.setRowCount(0);
	}
	NotaDao nDao=new NotaDao();
	List<Nota> listaNota=nDao.insereNota(not, es, ju, que);
	for(Nota not: listaNota){
		Object[] linha=new Object[3];
		linha[0]=not.getNota();
		linha[1]=not.ge
*/		
	//}
	//}
		
	//}

//}
